diff --git a/build.gradle b/build.gradle
index 7d2c806..c7660d9 100644
--- a/build.gradle
+++ b/build.gradle
@@ -41,27 +41,46 @@ kotlin {
     }
 }
 
-def getDevelopmentVersion() {
-    def output = new StringBuilder()
-    def error = new StringBuilder()
-    def gitShortHash = ["git", "-C", projectDir.toString(), "rev-parse", "--short", "HEAD"].execute()
-    gitShortHash.waitForProcessOutput(output, error)
-    def gitHash = output.toString().trim()
-    if (gitHash.isEmpty()) {
-        println "git hash is empty: error: ${error.toString()}"
-        throw new IllegalStateException("git hash could not be determined")
+def resolveVersion() {
+    def releaseVersion = System.env.RELEASE_VERSION
+    if (releaseVersion != null && !releaseVersion.trim().isEmpty()) {
+        return releaseVersion.trim()
     }
-    def version = "0.0.0-" + new SimpleDateFormat('yyyy-MM-dd\'T\'HH-mm-ss').format(new Date()) + "-" + gitHash
-    println "created development version: $version"
-    version
+
+    def dateStr = new SimpleDateFormat("yyyy-MM-dd'T'HH-mm-ss").format(new Date())
+
+    try {
+        def output = new StringBuilder()
+        def error = new StringBuilder()
+        def proc = ["git", "-C", projectDir.toString(), "rev-parse", "--short", "HEAD"].execute()
+        proc.waitForProcessOutput(output, error)
+
+        if (proc.exitValue() == 0) {
+            def gitHash = output.toString().trim()
+            if (!gitHash.isEmpty()) {
+                def version = "0.0.0-${dateStr}-${gitHash}"
+                println "created development version: $version"
+                return version
+            }
+        }
+    } catch (Exception ignored) {
+    }
+
+    return "0.0.0-local-${dateStr}"
 }
 
-def releaseVersion = System.env.RELEASE_VERSION
-version = releaseVersion ? releaseVersion : getDevelopmentVersion()
+version = resolveVersion()
 group = 'com.graphql-java'
 description = 'A pure Java 11 port of Facebook Dataloader'
 
+def printResolvedVersionRequested = gradle.startParameter.taskNames.any {
+    it == 'printResolvedVersion' || it.endsWith(':printResolvedVersion')
+}
+
 gradle.buildFinished { buildResult ->
+    if (printResolvedVersionRequested) {
+        return
+    }
     println "*******************************"
     println "*"
     if (buildResult.failure != null) {
@@ -74,6 +93,12 @@ gradle.buildFinished { buildResult ->
     println "*******************************"
 }
 
+task printResolvedVersion {
+    doLast {
+        println project.version
+    }
+}
+
 repositories {
     mavenCentral()
     mavenLocal()
